<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Rating" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/colorRating</item>
        <item name="colorControlActivated">@color/colorRating</item>
    </style>
    <style name="ProgressBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/colorProgressBar</item>
        <item name="colorControlActivated">@color/colorProgressBar</item>
    </style>

    <style name="LoginFields" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@color/colorLoginFieldsHints</item>
        <item name="android:textSize">20dp</item>
        <item name="colorControlActivated">@color/colorLoginFields</item>
    </style>
</resources>
